version: '3.4'

services:
  wpm.management.api:
    image: ${DOCKER_REGISTRY-}wpmmanagement
    build:
      context: .
      dockerfile: Wpm.Management.Api/Dockerfile

  wpm.clinic:
    image: ${DOCKER_REGISTRY-}wpm
    build:
      context: .
      dockerfile: Wpm.Clinic/Dockerfile

